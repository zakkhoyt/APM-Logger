CREATE TABLE 'FMT' (typeID integer PRIMARY KEY,length integer,name varchar(200),format varchar(6000),val varchar(6000));
CREATE TABLE 'PARM' (idx integer PRIMARY KEY,Name real,Value real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'GPS' (idx integer PRIMARY KEY,Status real,TimeMS real,Week real,NSats real,HDop real,Lat real,Lng real,RelAlt real,Alt real,Spd real,GCrs real,VZ real,T integer);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'IMU' (idx integer PRIMARY KEY,TimeMS real,GyrX real,GyrY real,GyrZ real,AccX real,AccY real,AccZ real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'MSG' (idx integer PRIMARY KEY,Message real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'ATUN' (idx integer PRIMARY KEY,Axis real,TuneStep real,RateMin real,RateMax real,RPGain real,RDGain real,SPGain real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'ATDE' (idx integer PRIMARY KEY,Angle real,Rate real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'CURR' (idx integer PRIMARY KEY,ThrOut real,ThrInt real,Volt real,Curr real,Vcc real,CurrTot real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'MOT' (idx integer PRIMARY KEY,Mot1 real,Mot2 real,Mot3 real,Mot4 real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'OF' (idx integer PRIMARY KEY,Dx real,Dy real,SQual real,X real,Y real,Lat real,Lng real,Roll real,Pitch real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'NTUN' (idx integer PRIMARY KEY,WPDst real,WPBrg real,PErX real,PErY real,DVelX real,DVelY real,VelX real,VelY real,DAcX real,DAcY real,DRol real,DPit real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'CTUN' (idx integer PRIMARY KEY,ThrIn real,SonAlt real,BarAlt real,WPAlt real,DesSonAlt real,AngBst real,CRate real,ThrOut real,DCRate real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'MAG' (idx integer PRIMARY KEY,MagX real,MagY real,MagZ real,OfsX real,OfsY real,OfsZ real,MOfsX real,MOfsY real,MOfsZ real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'PM' (idx integer PRIMARY KEY,RenCnt real,RenBlw real,NLon real,NLoop real,MaxT real,PMT real,I2CErr real,INSErr real,INAVErr real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'CMD' (idx integer PRIMARY KEY,CTot real,CNum real,CId real,COpt real,Prm1 real,Alt real,Lat real,Lng integer);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'ATT' (idx integer PRIMARY KEY,RollIn real,Roll real,PitchIn real,Pitch real,YawIn real,Yaw real,NavYaw real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'INAV' (idx integer PRIMARY KEY,BAlt real,IAlt real,IClb real,ACorrX real,ACorrY real,ACorrZ real,GLat real,GLng real,ILat real,ILng real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'MODE' (idx integer PRIMARY KEY,Mode real,ThrCrs real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'EV' (idx integer PRIMARY KEY,Id real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'D16' (idx integer PRIMARY KEY,Id real,Value real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'DU16' (idx integer PRIMARY KEY,Id real,Value real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'D32' (idx integer PRIMARY KEY,Id real,Value real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'DU32' (idx integer PRIMARY KEY,Id real,Value integer);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'DFLT' (idx integer PRIMARY KEY,Id real,Value real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'PID' (idx integer PRIMARY KEY,Id real,Error real,P real,I real,D real,Out real,Gain real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'CAM' (idx integer PRIMARY KEY,GPSTime real,GPSWeek real,Lat real,Lng real,Alt real,Roll real,Pitch real,Yaw real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
CREATE TABLE 'ERR' (idx integer PRIMARY KEY,Subsys real,ECode real);
INSERT INTO 'FMT' (typeID,length,name,format,val) values (?,?,?,?,?);
